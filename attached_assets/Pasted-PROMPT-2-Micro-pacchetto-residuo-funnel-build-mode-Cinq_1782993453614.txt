PROMPT 2 — Micro-pacchetto residuo funnel (build mode)
Cinque fix piccoli e indipendenti. Nessun refactor oltre a quanto elencato.

Feedback post-claim guest battle — In App.tsx (UserSync), quando la chiamata a /api/battles/guest/claim risponde OK, mostra un toast di successo localizzato IT/EN/ES (IT: "La tua battaglia è stata salvata nel tuo profilo ✓"). Se la risposta contiene un identificativo utile (battle/match/card id), aggiungi al toast un'azione "Vedi" che naviga alla pagina relativa (battle card o feed battaglie — usa quella che i dati permettono). Oggi il claim è completamente silenzioso: l'utente appena registrato non sa che la sua battaglia è stata salvata.
Pulizia param morto — In guest-battle.tsx il CTA post-vittoria punta a /sign-up?claimGuest=${guestId}, ma quel query param non viene letto da nessun codice (il claim reale passa da localStorage sgi-guest-claim). Rimuovi il param: il link diventa /sign-up.
CTA nell'empty state della dashboard — In dashboard.tsx, quando l'utente non ha cronologia (dove oggi appare dashboard.noHistory), aggiungi sotto il testo un bottone primario localizzato IT/EN/ES che porta a /chat?start=1 (riusa il meccanismo di auto-creazione conversazione già esistente). IT: "Inizia la tua prima conversazione". Il nuovo iscritto non deve trovarsi davanti a una dashboard vuota e passiva.
Errori di rete visibili nella guest battle — In guest-battle.tsx ci sono catch silenziosi (es. catch { /* ignore */ } in handleSend). Sostituiscili con un toast di errore localizzato IT/EN/ES (IT: "Errore di connessione — riprova"). L'utente su rete instabile oggi preme Invia e non riceve alcun feedback.
Locale nel checkout Stripe — Nel backend (billing.ts), verifica se la creazione della sessione di checkout passa il parametro locale a Stripe. Se no: accetta un campo lang dal client (aggiungilo alla chiamata in settings.tsx, prendendolo da i18n.language), valida che sia uno tra it/en/es e passalo come locale alla sessione Stripe (fallback: "auto"). Così il checkout e le ricevute escono nella lingua dell'utente.

Vincoli: tre lingue perfettamente parallele in i18n.ts per ogni nuova stringa; zero errori TypeScript; nessuna modifica fuori dai file citati. Report finale punto per punto con i file toccati.